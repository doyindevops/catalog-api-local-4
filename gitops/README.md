# GitOps Handoff Notes

This service is intended to be deployed through the GitOps flow.

## Expected next step

Add the Kubernetes manifests from the `k8s/` folder into the GitOps repository path that ArgoCD watches for the target environment.
